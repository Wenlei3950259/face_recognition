# 系统修改记录

## 修改日期
2026-01-12

## 修改内容
- 添加了各个说明文档

修改的文件：
- api/face_recognition_api.py
- core/face_core.py
- face_process/init_InsightFace.py
- config/__init__.py

### 2. 项目名称修改
所有相关的词汇已修改为 "recognition"（识别）

#### 主要修改项：

**文件重命名：**
- `api/face_medical_api.py` → `api/face_recognition_api.py`
- `log/face_medical.log` → `log/face_recognition.log`

**配置文件修改：**
- `config/config.yaml`: 日志文件路径更新为 `log/face_recognition.log`

**代码文件修改：**
- `start_server.py`: 导入模块路径更新为 `api.face_recognition_api`
- `api/face_recognition_api.py`: 
  - FastAPI 应用标题改为 "人脸识别系统 API"
  - 健康检查接口返回 `"service": "face-recognition-api"`
  - 日志文件路径更新
- `check_compatibility.py`: 导入模块和环境名称更新
- `diagnose.py`: 日志文件路径更新
- `start_daemon.py`: 日志文件路径更新

**文档文件修改：**
- `README.md`: 项目标题和所有 medical 引用改为 recognition
- `部署启动指南_win.md`: 所有 medical 引用改为 recognition
- `部署启动指南_ubuntu.md`: 所有 medical 引用改为 recognition
- `文档索引.md`: 所有 medical 引用改为 recognition
- `项目文件清单.txt`: 所有 medical 引用改为 recognition
- `依赖说明.md`: 所有 medical 引用改为 recognition
- 其他所有 .md 和 .txt 文件

**脚本文件修改：**
- 所有 .sh 和 .bat 脚本文件中的 medical 引用已更新

**环境名称建议：**
- 建议将 Conda 环境名从 `face_mobile_medical` 改为 `face_mobile_recognition`
- 文档中已更新相关引用

### 3. 项目路径建议
文档中的项目路径已从 `/home/face/face_medical` 更新为 `/home/face/face_recognition`

## 验证结果
✅ API 文件已重命名并更新所有引用
✅ 配置文件已更新
✅ 文档已全部更新

## 注意事项

1. **日志文件**: 旧的日志文件 `log/face_recognition.log` 保留了历史记录，其中包含旧的 API 文件名引用，这是正常的
2. **IDE 配置**: `.idea` 目录下的配置文件可能仍包含旧的引用，不影响系统运行
3. **环境迁移**: 如需创建新的 Conda 环境，建议使用名称 `face_mobile_recognition`
4. **重新部署**: 修改后建议重新启动服务以使所有更改生效

## 启动服务

修改完成后，使用以下命令启动服务：

**Windows:**
```bash
start_server_win.bat
```

**Ubuntu:**
```bash
python start_server.py
```

服务启动后，访问 http://localhost:5000/health 验证服务状态。
